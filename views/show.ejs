<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> Search </title>
</head>
<body>
    <div class="container">
        <% for (let repo of repos) { %>
            <h2> <a href=" <%= repo.html_url %> "> <%= repo.name %> </a> </h2>
            <h3> Owner : <a href="<%= repo.owner.html_url %>"> <%= repo.owner.login %> </a> </h3>
        <% } %>
    </div>
</body>
</html>